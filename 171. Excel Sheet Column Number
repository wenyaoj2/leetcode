class Solution {
    public int titleToNumber(String s) {
        int len = 0;
        int sum = 0;
        for(int i = 0; i<s.length();i++){
            len = s.charAt(i)-'A'+1;
            sum = (int)Math.pow(26,(s.length()-1-i))*len+sum;
        }
        return sum;
    }
}
